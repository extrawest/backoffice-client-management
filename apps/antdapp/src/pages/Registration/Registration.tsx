import { FC } from "react";
import { RegistrationContainer } from "@mono-redux-starter/antdapplib";

export const Registration: FC = () => (
	<RegistrationContainer />
);

export default Registration;
