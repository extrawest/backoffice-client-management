import { FC } from "react";
import { RegistrationContainer } from "@mono-redux-starter/semanticapplib";

export const Registration: FC = () => (
	<RegistrationContainer />
);

export default Registration;
