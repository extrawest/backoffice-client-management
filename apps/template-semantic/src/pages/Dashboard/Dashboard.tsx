import { FC } from "react";
import { PrimeDashboardContainer } from "@mono-redux-starter/tamplateapp";

export const Dashboard: FC = () =>  (
	<PrimeDashboardContainer />
);

export default Dashboard;
