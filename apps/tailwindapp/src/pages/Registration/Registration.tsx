import { FC } from "react";
import { RegistrationContainer } from "@mono-redux-starter/tailwind-lib";

export const Registration: FC = () => (
	<RegistrationContainer />
);

export default Registration;
