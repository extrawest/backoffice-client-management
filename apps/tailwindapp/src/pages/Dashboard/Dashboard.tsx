import { FC } from "react";
import { DashboardContainer } from "@mono-redux-starter/tailwind-lib";

export const Dashboard: FC = () => {
	return (
		<>
			<DashboardContainer />
		</>
	);
};

export default Dashboard;
