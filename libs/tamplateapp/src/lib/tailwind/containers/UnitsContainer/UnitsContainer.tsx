import { FC } from "react";
import { useIntl } from "react-intl";
import { Typography } from "@mui/material";

export const TailwindUnitsContainer: FC = () => {
	const intl = useIntl();

	return (
		<>
			32131
		</>
	);
};
