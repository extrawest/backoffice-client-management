import { LoginContainer } from "@mono-redux-starter/primeapplib";
import { FC } from "react";

const Login: FC = () => {
	return (
		<div>
			<LoginContainer />
		</div>
	);
};

export default Login;
