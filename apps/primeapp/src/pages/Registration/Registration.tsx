import { FC } from "react";
import { RegistrationContainer } from "@mono-redux-starter/primeapplib";

export const Registration: FC = () => (
	<RegistrationContainer />
);

export default Registration;
